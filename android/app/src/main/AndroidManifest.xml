<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Sawata"
        android:name=".SawataApplication"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

            <!-- "Open Sawatâ to Guardian Invites" deep link — a custom URI
                 scheme, NOT a verified Android App Link. App Links require
                 owning a real HTTPS domain that hosts a signed Digital
                 Asset Links file (/.well-known/assetlinks.json) so Android
                 can verify this app is the legitimate handler; this
                 project has no such domain. A custom scheme has no such
                 verification: any other installed app could register the
                 same "sawata" scheme, in which case Android would show an
                 app-chooser instead of always opening Sawatâ directly.
                 Deliberately generic/not invitation-specific — it just
                 opens the existing Guardian Invites screen where the
                 guardian reviews and Accepts/Rejects in-app. Handled in
                 Dart via the app_links package — see
                 lib/services/deep_link_service.dart. -->
            <meta-data android:name="flutter_deeplinking_enabled" android:value="false" />
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="sawata" android:host="guardian-invites" />
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <activity
            android:name=".BlockScreenActivity"
            android:exported="false"
            android:launchMode="singleTask"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme" />

        <service
            android:name=".SawataAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".vpn.SawataVpnService"
            android:exported="false"
            android:permission="android.permission.BIND_VPN_SERVICE">
            <intent-filter>
                <action android:name="android.net.VpnService" />
            </intent-filter>
        </service>

        <!-- PACKAGE_ADDED is a protected system broadcast (only the OS can
             send it), so exported=true here doesn't let another app spoof
             it — this just satisfies Android 12+'s requirement that every
             manifest-registered receiver with an intent-filter declare
             android:exported explicitly. Enqueues AI classification work for
             newly-installed/updated apps — see AppInstallReceiver.kt. -->
        <receiver
            android:name=".AppInstallReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.PACKAGE_ADDED" />
                <data android:scheme="package" />
            </intent-filter>
        </receiver>
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- Lets InstalledAppsProvider.kt enumerate normal, launchable apps
             for the Blocked Sites & Apps "Choose an App" picker. Scoped to
             MAIN/LAUNCHER only (not QUERY_ALL_PACKAGES) — Android 11+'s
             package visibility rules hide every other installed app's
             existence from this query without this declaration. -->
        <intent>
            <action android:name="android.intent.action.MAIN"/>
            <category android:name="android.intent.category.LAUNCHER"/>
        </intent>
    </queries>
</manifest>
